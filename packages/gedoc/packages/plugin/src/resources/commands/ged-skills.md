---
description: Suggest and sync bundled GedOC skills for the current work
agent: gedoc
---
Interpret the current work from `$ARGUMENTS`.

Then:
- call `gedoc_suggest_skills`
- call `gedoc_update_skills`
- summarize which bundled skills should be used next and why
