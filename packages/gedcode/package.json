{
  "name": "gedcode",
  "private": true,
  "version": "0.3.0",
  "description": "GedCode monorepo: OpenCode plugin + launcher for the Omni workflow.",
  "repository": {
    "type": "git",
    "url": "https://github.com/edgyarmati/ged-mono"
  },
  "homepage": "https://github.com/edgyarmati/ged-mono#readme",
  "bugs": {
    "url": "https://github.com/edgyarmati/ged-mono/issues"
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @gedcode/plugin --if-present",
    "check": "node scripts/sync-version.mjs --check && npm run check -w @gedcode/plugin -w @gedcode/launcher --if-present",
    "setup": "./scripts/setup",
    "sync-version": "node scripts/sync-version.mjs",
    "test": "npm run test -w @gedcode/plugin -w @gedcode/launcher --if-present"
  },
  "engines": {
    "node": ">=22"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "typescript": "^6.0.3"
  }
}
