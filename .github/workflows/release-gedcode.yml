name: Release GedCode

on:
  push:
    tags:
      - "gedcode-v*"
  workflow_dispatch:
    inputs:
      version:
        description: "Release version without prefix (e.g. 0.4.0)"
        required: true
        type: string

permissions:
  contents: write

jobs:
  build-and-release:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install dependencies
        run: npm ci

      - name: Extract version
        id: version
        run: |
          if [[ "${GITHUB_REF_NAME}" == gedcode-v* ]]; then
            echo "VERSION=${GITHUB_REF_NAME#gedcode-v}" >> "$GITHUB_OUTPUT"
          else
            echo "VERSION=${{ github.event.inputs.version }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Type-check and sync versions
        run: npm -w packages/gedcode run check

      - name: Test
        run: npm -w packages/gedcode test

      - name: Verify version matches
        run: |
          PKG_VERSION=$(node -e "console.log(require('./package.json').version)")
          if [ "$PKG_VERSION" != "${{ steps.version.outputs.VERSION }}" ]; then
            echo "Package version ($PKG_VERSION) does not match release version (${{ steps.version.outputs.VERSION }})"
            exit 1
          fi
        working-directory: packages/gedcode

      - name: Build release bundle
        env:
          GEDCODE_VERSION: ${{ steps.version.outputs.VERSION }}
        run: bash scripts/release/bundle.sh
        working-directory: packages/gedcode

      - name: Compute SHA256SUMS
        run: |
          cd packages/gedcode/dist
          shasum -a 256 gedcode-*.tar.gz > SHA256SUMS
          cat SHA256SUMS

      - name: Copy installer scripts
        run: |
          cp packages/gedcode/install.sh packages/gedcode/dist/install.sh
          cp packages/gedcode/install.ps1 packages/gedcode/dist/install.ps1

      - name: Determine release tag
        id: tag
        run: |
          if [[ "${GITHUB_REF_NAME}" == gedcode-v* ]]; then
            echo "TAG=${GITHUB_REF_NAME}" >> "$GITHUB_OUTPUT"
          else
            echo "TAG=gedcode-v${{ steps.version.outputs.VERSION }}" >> "$GITHUB_OUTPUT"
          fi

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ steps.tag.outputs.TAG }}
          files: |
            packages/gedcode/dist/gedcode-*.tar.gz
            packages/gedcode/dist/SHA256SUMS
            packages/gedcode/dist/install.sh
            packages/gedcode/dist/install.ps1
          generate_release_notes: true
